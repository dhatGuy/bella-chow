import { Box, Button } from "@chakra-ui/react";

const Menus = () => {
  return (
    <Box>
      <Button>Dashboard</Button>
      <Box>
        <Button onClick={() => {}}>Add Menu</Button>
      </Box>
    </Box>
  );
};

export default Menus;
